apiVersion: v1
kind: ConfigMap
metadata:
  name: airport-config
  namespace: airport
data:
  DB_URL: jdbc:oracle:thin:@oracle:1521/FREEPDB1
  CORS_ALLOWED_ORIGINS: https://airport.example.com
  PAYMENT_APPROVAL_RATE: "0.92"
  MAIL_HOST: mailhog
  MAIL_PORT: "1025"
  MAIL_FROM: reservas@tagairlines.local
  MAIL_SMTP_AUTH: "false"
  MAIL_SMTP_STARTTLS: "false"
